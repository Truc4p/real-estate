import AlertsList from '@/components/alerts/AlertsList';

export const metadata = {
  title: 'Alerts & Notifications | Real Estate',
  description: 'View your property alerts and notifications',
};

export default function AlertsPage() {
  return <AlertsList />;
}
